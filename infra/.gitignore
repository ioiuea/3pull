# Compiled ARM templates from bicep build
bicep/*.json

# Keep params and logs directory only; ignore all generated files under it
params/*
!params/.gitkeep

logs/*
!logs/.gitkeep

# Python bytecode caches
scripts/__pycache__
scripts/**/*.pyc

common.parameter.test.json
