# -----------------------------------------------------------------------------
# Auto-generated file
# -----------------------------------------------------------------------------
# このファイルは `infra/main.sh` から自動生成されます。
# 直接編集しても次回生成時に上書きされます。
#
# 使い方:
# 1. image.tag の `__IMAGE_TAG__` を CI で実際のタグに置き換えます
#    例: git sha やリリースタグ
# 2. 変更したファイルを使って `helm upgrade --install` を実行します
# -----------------------------------------------------------------------------
systemName: '3pull'

# Frontend Deployment 設定
# image.repository はインフラ側で自動更新されます。
# image.tag は CI で毎回置き換えてください。
frontend:
  enabled: true
  replicaCount: 1
  image:
    repository: 'crdev3pull.azurecr.io/3pull-web'
    tag: '__IMAGE_TAG__'
    pullPolicy: IfNotPresent
  service:
    type: ClusterIP
    port: 3000
    targetPort: 3000
  probes:
    readiness:
      path: /
      initialDelaySeconds: 3
      periodSeconds: 10
    liveness:
      path: /
      initialDelaySeconds: 10
      periodSeconds: 15
  podAnnotations: {}
  resources: {}
