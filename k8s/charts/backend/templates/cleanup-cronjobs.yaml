{{- $root := . -}}
{{- range $name, $job := .Values.cleanup.jobs }}
{{- if $job.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "backend.cleanupName" $root }}-{{ $name | kebabcase }}
  namespace: {{ include "backend.namespace" $root }}
  labels:
    {{- include "backend.labels" $root | nindent 4 }}
    app.kubernetes.io/component: cleanup
    app.kubernetes.io/part-of: {{ $name | kebabcase }}
spec:
  schedule: {{ $job.schedule | quote }}
  suspend: {{ $job.suspend }}
  concurrencyPolicy: {{ $job.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ $root.Values.cleanup.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ $root.Values.cleanup.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            {{- include "backend.selectorLabels" $root | nindent 12 }}
            app.kubernetes.io/component: cleanup
            app.kubernetes.io/part-of: {{ $name | kebabcase }}
            azure.workload.identity/use: "true"
          {{- with $job.podAnnotations }}
          annotations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
        spec:
          restartPolicy: OnFailure
          serviceAccountName: {{ $root.Values.serviceAccounts.cleanup.name }}
          containers:
            - name: {{ $name | kebabcase }}
              image: "{{ $root.Values.cleanup.image.repository }}:{{ $root.Values.cleanup.image.tag }}"
              imagePullPolicy: {{ $root.Values.cleanup.image.pullPolicy }}
              env:
                - name: CLEANUP_COMMAND
                  value: {{ $job.command | quote }}
              envFrom:
                - configMapRef:
                    name: {{ include "backend.apiName" $root }}-config
                - secretRef:
                    name: {{ $root.Values.secretRefs.name }}
              resources:
                {{- toYaml $job.resources | nindent 16 }}
---
{{- end }}
{{- end }}
