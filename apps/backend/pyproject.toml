[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.131.0",
    "structlog>=25.5.0",
]

[dependency-groups]
dev = [
    "uvicorn>=0.41.0",
]

[project.scripts]
backend = "app.main:main"

[tool.uv]
package = true
