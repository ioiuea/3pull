[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.131.0",
    "gunicorn>=25.1.0",
    "pydantic-settings>=2.13.1",
    "structlog>=25.5.0",
]

[dependency-groups]
dev = [
    "python-dotenv>=1.2.1",
    "uvicorn>=0.41.0",
]

[tool.uv]
package = true
