# Infra タスク整理

## 検討事項

### App Gateway の二重化オプション

- 検討内容:
  - 通常の `App Gateway` に加えて、`Firewall 経路の UDR を付けない App Gateway` を別系統で持つ二重化オプションを検討する。
- 主な観点:
  - 障害時の迂回経路として使うのか
  - 常時 active/active にするのか
  - FW 経由あり / なしで、どこまで経路差を許容するか
  - DNS / 切替手順 / 運用監視をどうするか
- 未確定事項:
  - 正式に採用するか
  - 採用時の命名・サブネット・ルーティング分離
